// LeetCode 12: Integer to Roman
// Greedy conversion using descending value-symbol pairs.

fn int_to_roman(num: int) -> str {
    amounts: list = [1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1];
    symbols: list = ["M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"];

    n: int = num;
    result: str = "";

    amounts.zip(symbols).forEach(fn(pair: list) {
        use mut n, result;
        [value: int, symbol: str] = pair;
        while n >= value {
            result = result + symbol;
            n = n - value;
        }
    });

    return result;
}

say(int_to_roman(3749));
say(int_to_roman(58));
say(int_to_roman(1994));

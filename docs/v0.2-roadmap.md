# Echo v0.2 Roadmap

v0.2 is an architecture and correctness release.

## Done

- Language semantics documented
- Typed AST
- Semantic analyzer
- Lexical scoping and closures
- Predictable `use mut` in nested blocks
- Echo error types and source locations
- Builtins removed from the lexer
- Regression tests and CI
- Version `0.2.0`

## Frozen

The v0.2 pipeline is frozen unless a stress test proves the architecture itself is wrong.

Known leftover work is **semantic debt**, not architecture work. Track it in [v0.2.1-backlog.md](v0.2.1-backlog.md).

## Not in v0.2

- Modules and packages
- Classes / generics / macros
- Bytecode VM
- HTTP / database libraries
- LSP / formatter

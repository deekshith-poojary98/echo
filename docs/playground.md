---
layout: page
sidebar: false
aside: false
title: Playground
description: Write and run Echo in the browser
---

Browser runner for the docs site. Restricted host: no files, no `run`. For a full session (bindings, `import`, files), use the CLI REPL (`echo` with no file).

<Playground />
